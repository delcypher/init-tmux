# Commands in this file are executed when the session is initialised by shell
# running the init-tmux command. A variable ${INIT_TMUX_SESSION} is passed to
# the environment which contains the name of the session being initialised.

# Example creating two windows into a single pane
tmux split-window -h -t "${INIT_TMUX_SESSION}:0" -c /
